info:
  name: Get Album Images
  type: http
  seq: 6
  tags:
    - Albums

http:
  method: POST
  url: "{{baseUrl}}/albums/:album_id/images/get"
  params:
    - name: album_id
      value: ""
      type: path
  body:
    type: json
    data: |-
      {
        "password": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Successful Response
    request:
      url: "{{baseUrl}}/albums/:album_id/images/get"
      method: POST
      params:
        - name: album_id
          value: ""
          type: path
      body:
        type: json
        data: |-
          {
            "password": ""
          }
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "success": false,
            "image_ids": []
          }
  - name: 422 Response
    description: Validation Error
    request:
      url: "{{baseUrl}}/albums/:album_id/images/get"
      method: POST
      params:
        - name: album_id
          value: ""
          type: path
      body:
        type: json
        data: |-
          {
            "password": ""
          }
    response:
      status: 422
      statusText: Unprocessable Entity
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "detail": [
              {
                "loc": [],
                "msg": "",
                "type": ""
              }
            ]
          }
